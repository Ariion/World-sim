# GENESIS ENGINE — .env.example
# Copier en .env et remplir les valeurs

# PostgreSQL (fourni automatiquement par Railway)
DATABASE_URL=postgresql://user:password@host:5432/genesis

# Serveur
HOST=0.0.0.0
PORT=8000

# Monde (changer le seed = monde différent)
WORLD_SEED=42
